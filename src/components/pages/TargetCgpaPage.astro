---
// src/components/pages/TargetCgpaPage.astro
import Layout from '../../layouts/Layout.astro';
import TargetCgpaCalculator from '../TargetCgpaCalculator.astro';
import { useTranslations, useTranslatedPath, type Lang } from '../../i18n/utils';

interface Props {
  lang?: Lang;
}

const { lang = 'en' } = Astro.props;
const t = useTranslations(lang);
const translatePath = useTranslatedPath(lang);

const breadcrumbs = [
  { name: t('nav.home'), item: translatePath('/') },
  { name: t('menu.target_cgpa'), item: translatePath('/target-cgpa-calculator') }
];

const faqSchema = [
  {
    question: t('targetcgpa.faq1_q'),
    answer: t('targetcgpa.faq1_a')
  },
  {
    question: t('targetcgpa.faq2_q'),
    answer: t('targetcgpa.faq2_a')
  },
  {
    question: t('targetcgpa.faq3_q'),
    answer: t('targetcgpa.faq3_a')
  },
  {
    question: t('targetcgpa.faq4_q'),
    answer: t('targetcgpa.faq4_a')
  }
];
---

<Layout
  title={t('targetcgpa.title')}
  description={t('targetcgpa.desc')}
  breadcrumbs={breadcrumbs}
  faqSchema={faqSchema}
  lang={lang}
  softwareSchema={{
    name: t('menu.target_cgpa'),
    description: t('targetcgpa.desc'),
    applicationCategory: "EducationalApplication"
  }}
>
  <div class="container" style="max-width: 960px; padding-top: 2.5rem; padding-bottom: 5rem;">
    <!-- Breadcrumbs -->
    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1.5rem; flex-wrap: wrap; gap: 0.75rem;">
      <nav aria-label="Breadcrumb" style="font-size: 0.8125rem; color: var(--color-mute);">
        <a href={translatePath('/')} style="color: var(--color-mute); text-decoration: none;" data-i18n="nav.home">{t('nav.home')}</a>
        <span style="margin: 0 0.5rem; color: var(--color-hairline);">&gt;</span>
        <span style="color: var(--color-ink); font-weight: 500;" data-i18n="menu.target_cgpa">{t('menu.target_cgpa')}</span>
      </nav>
      <div style="display: flex; gap: 0.5rem;">
        <a href={translatePath('/target-gpa-calculator')} class="btn btn-secondary btn-pill" style="font-size: 0.8125rem; text-decoration: none;">
          <span data-i18n="menu.target_gpa">{t('menu.target_gpa')}</span> &rarr;
        </a>
      </div>
    </div>

    <!-- Header Section -->
    <header style="margin-bottom: 2rem; border-bottom: 1px solid var(--color-hairline); padding-bottom: 1.5rem;">
      <span class="mono-eyebrow" style="color: var(--color-violet); margin-bottom: 0.35rem;" data-i18n="targetcgpa.eyebrow">{t('targetcgpa.eyebrow')}</span>
      <h1 class="heading-lg" style="color: var(--color-ink); font-size: clamp(1.85rem, 4vw, 2.5rem); letter-spacing: -0.02em; margin-top: 0.25rem;" data-i18n="targetcgpa.title">
        {t('targetcgpa.title')}
      </h1>
      <p style="font-size: 1.05rem; color: var(--color-body); margin-top: 0.75rem; line-height: 1.6; max-width: 780px;" data-i18n="targetcgpa.desc">
        {t('targetcgpa.desc')}
      </p>
    </header>

    <!-- Interactive Calculator -->
    <section style="margin-bottom: 3rem;">
      <TargetCgpaCalculator idPrefix="page-targetcgpa" lang={lang} />
    </section>

    <!-- Guide & Formula Section -->
    <article class="geist-card" style="padding: 2.25rem 2rem; line-height: 1.75; color: var(--color-body); margin-bottom: 2rem;">
      <h2 class="heading-md" style="color: var(--color-ink); font-size: 1.35rem; margin-bottom: 1rem;" data-i18n="targetcgpa.guide_heading">
        {t('targetcgpa.guide_heading')}
      </h2>
      <p style="margin-bottom: 1rem;" data-i18n="targetcgpa.guide_p1">
        {t('targetcgpa.guide_p1')}
      </p>
      <div style="background: var(--color-canvas-subtle); padding: 1rem 1.25rem; border-radius: var(--radius-sm); border: 1px solid var(--color-hairline); font-family: var(--font-mono); font-size: 0.875rem; margin-bottom: 1.25rem;" data-i18n="targetcgpa.formula">
        {t('targetcgpa.formula')}
      </div>

      <h3 class="heading-md" style="font-size: 1.15rem; color: var(--color-ink); margin-bottom: 0.75rem;" data-i18n="targetcgpa.handling_heading">
        {t('targetcgpa.handling_heading')}
      </h3>
      <p style="margin-bottom: 1rem;" data-i18n="targetcgpa.handling_p1">
        {t('targetcgpa.handling_p1')}
      </p>
      <p data-i18n="targetcgpa.handling_p2">
        {t('targetcgpa.handling_p2')}
      </p>
    </article>

    <!-- FAQs -->
    <section class="faq-card" style="margin-bottom: 3rem;">
      <h2 class="heading-md" style="margin-bottom: 1.25rem; color: var(--color-ink);" data-i18n="targetcgpa.faq_heading">
        {t('targetcgpa.faq_heading')}
      </h2>
      <div style="display: flex; flex-direction: column; gap: 1rem;">
        <div class="faq-item">
          <div class="faq-question" data-i18n="targetcgpa.faq1_q">{t('targetcgpa.faq1_q')}</div>
          <div class="faq-answer" data-i18n="targetcgpa.faq1_a">
            {t('targetcgpa.faq1_a')}
          </div>
        </div>
        <div class="faq-item">
          <div class="faq-question" data-i18n="targetcgpa.faq2_q">{t('targetcgpa.faq2_q')}</div>
          <div class="faq-answer" data-i18n="targetcgpa.faq2_a">
            {t('targetcgpa.faq2_a')}
          </div>
        </div>

        <div class="faq-item">
          <div class="faq-question" data-i18n="targetcgpa.faq3_q">{t('targetcgpa.faq3_q')}</div>
          <div class="faq-answer" data-i18n="targetcgpa.faq3_a">
            {t('targetcgpa.faq3_a')}
          </div>
        </div>

        <div class="faq-item">
          <div class="faq-question" data-i18n="targetcgpa.faq4_q">{t('targetcgpa.faq4_q')}</div>
          <div class="faq-answer" data-i18n="targetcgpa.faq4_a">
            {t('targetcgpa.faq4_a')}
          </div>
        </div>
      </div>
    </section>

    <!-- Related Links -->
    <section>
      <h3 class="heading-md" style="margin-bottom: 1rem; color: var(--color-ink);" data-i18n="targetcgpa.related_heading">{t('targetcgpa.related_heading')}</h3>
      <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 1rem;">
        <a href={translatePath('/target-gpa-calculator')} class="geist-card" style="padding: 1.25rem; text-decoration: none; display: block;">
          <h4 style="font-size: 0.9375rem; font-weight: 600; color: var(--color-ink); margin-bottom: 0.25rem;" data-i18n="menu.target_gpa">{t('menu.target_gpa')}</h4>
          <p style="font-size: 0.8125rem; color: var(--color-mute); line-height: 1.4;" data-i18n="targetgpa.desc">{t('targetgpa.desc')}</p>
        </a>
        <a href={translatePath('/indian-cgpa-calculator')} class="geist-card" style="padding: 1.25rem; text-decoration: none; display: block;">
          <h4 style="font-size: 0.9375rem; font-weight: 600; color: var(--color-ink); margin-bottom: 0.25rem;" data-i18n="menu.icgpa">{t('menu.icgpa')}</h4>
          <p style="font-size: 0.8125rem; color: var(--color-mute); line-height: 1.4;" data-i18n="icgpa.desc">{t('icgpa.desc')}</p>
        </a>
        <a href={translatePath('/semester-cgpa-calculator')} class="geist-card" style="padding: 1.25rem; text-decoration: none; display: block;">
          <h4 style="font-size: 0.9375rem; font-weight: 600; color: var(--color-ink); margin-bottom: 0.25rem;" data-i18n="menu.sem_cgpa">{t('menu.sem_cgpa')}</h4>
          <p style="font-size: 0.8125rem; color: var(--color-mute); line-height: 1.4;" data-i18n="semcgpa.desc">{t('semcgpa.desc')}</p>
        </a>
      </div>
    </section>
  </div>
</Layout>
