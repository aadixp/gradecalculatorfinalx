---
// src/layouts/Layout.astro
import '../styles/geist.css';
import LanguagePicker from '../components/LanguagePicker.astro';
import CalculatorsMenu from '../components/CalculatorsMenu.astro';
import CourseModal from '../components/CourseModal.astro';
import { getLangFromUrl, useTranslations, useTranslatedPath, getAlternateUrls } from '../i18n/utils';
import type { Lang } from '../i18n/ui';

interface Props {
	title?: string;
	description?: string;
	canonical?: string;
	lang?: Lang;
	breadcrumbs?: Array<{ name: string; item: string }>;
	faqSchema?: Array<{ question: string; answer: string }>;
	softwareSchema?: {
		name: string;
		description: string;
		applicationCategory?: string;
	};
}

const urlLang = getLangFromUrl(Astro.url);
const currentLang = Astro.props.lang || urlLang;
const t = useTranslations(currentLang);
const translatePath = useTranslatedPath(currentLang);

const {
	title = t('site.title'),
	description = t('site.description'),
	canonical,
	breadcrumbs,
	faqSchema,
	softwareSchema
} = Astro.props;

const canonicalUrl = canonical || (currentLang === 'en'
	? `https://gradecalculatorfinalx.com${Astro.url.pathname === '/' ? '/' : (Astro.url.pathname.endsWith('/') ? Astro.url.pathname : Astro.url.pathname + '/')}`
	: `https://gradecalculatorfinalx.com${Astro.url.pathname.endsWith('/') ? Astro.url.pathname : Astro.url.pathname + '/'}`);
const alternateUrls = getAlternateUrls(canonicalUrl);

const ogLocales: Record<string, string> = {
	en: 'en_US',
	es: 'es_ES',
	ja: 'ja_JP',
	fr: 'fr_FR',
	de: 'de_DE',
	pt: 'pt_PT',
	ko: 'ko_KR',
	it: 'it_IT',
};
---

<!doctype html>
<html lang={currentLang} class="dark">
	<head>
		<!-- Google tag (gtag.js) -->
		<script async src="https://www.googletagmanager.com/gtag/js?id=G-3ZS11DPE2L" is:inline></script>
		<script is:inline>
		  window.dataLayer = window.dataLayer || [];
		  function gtag(){dataLayer.push(arguments);}
		  gtag('js', new Date());

		  gtag('config', 'G-3ZS11DPE2L');
		</script>

		<!-- Ahrefs Analytics -->
		<script src="https://analytics.ahrefs.com/analytics.js" data-key="mgoNoOyhSxRHAI4byY/wyQ" async is:inline></script>

		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<link rel="icon" type="image/png" href="/favicon-96x96.png" sizes="96x96" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="shortcut icon" href="/favicon.ico" />
		<link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
		<meta name="apple-mobile-web-app-title" content="GradeCalc" />
		<link rel="manifest" href="/site.webmanifest" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
		<meta name="description" content={description} />
		<meta name="keywords" content={t('site.keywords')} />
		<link rel="canonical" href={canonicalUrl} />
		<meta name="robots" content="index, follow" />
		<link rel="sitemap" type="application/xml" href="/sitemap-index.xml" />
		
		<!-- Multilingual Hreflang Markup for Search Engine Discovery -->
		{alternateUrls.map((alt) => (
			<link rel="alternate" hreflang={alt.hreflang} href={alt.href} />
		))}
		
		<!-- Open Graph / Social Sharing (WhatsApp, Instagram, Telegram, Facebook) -->
		<meta property="og:type" content="website" />
		<meta property="og:site_name" content="GradeCalculatorFinalX" />
		<meta property="og:url" content={canonicalUrl} />
		<meta property="og:locale" content={ogLocales[currentLang] || 'en_US'} />
		<meta property="og:title" content={title} />
		<meta property="og:description" content={description} />
		<meta property="og:image" content="https://gradecalculatorfinalx.com/og-image.png" />
		<meta property="og:image:secure_url" content="https://gradecalculatorfinalx.com/og-image.png" />
		<meta property="og:image:width" content="1200" />
		<meta property="og:image:height" content="630" />
		<meta property="og:image:type" content="image/png" />
		<meta property="og:image:alt" content="GradeCalculatorFinalX - Free Academic Grade Planner" />
		<link rel="image_src" href="https://gradecalculatorfinalx.com/og-image.png" />
		
		<!-- Twitter Card -->
		<meta name="twitter:card" content="summary_large_image" />
		<meta name="twitter:title" content={title} />
		<meta name="twitter:description" content={description} />
		<meta name="twitter:image" content="https://gradecalculatorfinalx.com/og-image.png" />
		<meta name="twitter:image:alt" content="GradeCalculatorFinalX - Free Academic Grade Planner" />

		<!-- JSON-LD Structured Data -->
		<script type="application/ld+json" is:inline set:html={JSON.stringify({
			"@context": "https://schema.org",
			"@graph": [
				{
					"@type": "Organization",
					"@id": "https://gradecalculatorfinalx.com/#organization",
					"name": "GradeCalculatorFinalX",
					"url": "https://gradecalculatorfinalx.com/",
					"logo": "https://gradecalculatorfinalx.com/favicon-96x96.png"
				},
				{
					"@type": "WebSite",
					"@id": "https://gradecalculatorfinalx.com/#website",
					"name": "GradeCalculatorFinalX",
					"url": "https://gradecalculatorfinalx.com/",
					"publisher": {
						"@id": "https://gradecalculatorfinalx.com/#organization"
					}
				},
				{
					"@type": softwareSchema ? "SoftwareApplication" : "WebApplication",
					"name": softwareSchema?.name || title,
					"url": canonicalUrl,
					"description": softwareSchema?.description || description,
					"applicationCategory": softwareSchema?.applicationCategory || "EducationalApplication",
					"operatingSystem": "All",
					"offers": {
						"@type": "Offer",
						"price": "0",
						"priceCurrency": "USD"
					},
					"featureList": [
						"Weighted Coursework Grade Calculator",
						"Final Exam Required Score Planner",
						"College & High School GPA Calculator (4.0 & 5.0 Scales)",
						"Indian CGPA & SGPA Calculator (UGC & AICTE CBCS 10.0 Scale)",
						"Semester-wise Multi-Term CGPA Calculator",
						"CGPA to Percentage and Percentage to CGPA Conversion",
						"Target GPA & Target CGPA Goal Planning",
						"Academic Grade Equivalence Matrix"
					]
				},
				...(breadcrumbs && breadcrumbs.length > 0 ? [{
					"@type": "BreadcrumbList",
					"itemListElement": breadcrumbs.map((b, idx) => ({
						"@type": "ListItem",
						"position": idx + 1,
						"name": b.name,
						"item": b.item.startsWith('http') ? b.item : `https://gradecalculatorfinalx.com${b.item === '/' ? '/' : (b.item.endsWith('/') ? b.item : b.item)}`
					}))
				}] : []),
				...(faqSchema && faqSchema.length > 0 ? [{
					"@type": "FAQPage",
					"mainEntity": faqSchema.map(f => ({
						"@type": "Question",
						"name": f.question,
						"acceptedAnswer": {
							"@type": "Answer",
							"text": f.answer
						}
					}))
				}] : [])
			]
		})} />

		<slot name="head" />
		
		<!-- Google Fonts for Geist & Geist Mono -->
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link href="https://fonts.googleapis.com/css2?family=Geist+Mono:wght@400;500;600&family=Geist:wght@400;500;600;700&display=swap" rel="stylesheet" />

		<!-- Inline script for early theme restoration to prevent flashing -->
		<script is:inline>
			(function() {
				try {
					const savedTheme = localStorage.getItem('gradecalc_theme');
					if (savedTheme === 'light') {
						document.documentElement.classList.remove('dark');
					} else if (savedTheme === 'dark') {
						document.documentElement.classList.add('dark');
					} else if (window.matchMedia && window.matchMedia('(prefers-color-scheme: light)').matches) {
						document.documentElement.classList.remove('dark');
					}
				} catch (e) {}
			})();
		</script>
	</head>
	<body>
		<!-- Header / Navigation Bar -->
		<header class="site-header">
			<div class="container site-header-inner">
				<!-- Logo & Course Name -->
				<div class="header-left-group">
					<a href={translatePath('/')} class="header-brand">
						<div class="header-brand-icon">
							<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
								<path d="M22 10v6M2 10l10-5 10 5-10 5z"></path>
								<path d="M6 12v5c3 3 9 3 12 0v-5"></path>
							</svg>
						</div>
						<span class="header-brand-title" data-i18n="nav.brand">{t('nav.brand')}</span>
					</a>

					<span class="header-divider">/</span>

					<!-- Active Course Selector -->
					<div class="header-course-wrap">
						<select id="course-selector" class="header-course-select" aria-label="Select Course Profile">
							<option value="default" data-i18n="calc.defaultCourse">{t('calc.defaultCourse')}</option>
						</select>
						<button id="add-course-profile-btn" type="button" class="header-course-add-btn" title="Create New Course" aria-label="Add Course" data-i18n-title="modal.createNewCourse">
							<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
								<line x1="12" y1="5" x2="12" y2="19"></line>
								<line x1="5" y1="12" x2="19" y2="12"></line>
							</svg>
						</button>
					</div>
				</div>

				<!-- Right Action Controls -->
				<div class="header-actions-group">
					<!-- Calculators Menu -->
					<CalculatorsMenu />

					<!-- Scale Button -->
					<button id="open-scale-modal-btn" type="button" class="header-action-btn" title="Grading Scale Cutoffs" aria-label="Grading Scale Cutoffs" data-i18n-title="modal.scaleTitle">
						<svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
							<path d="M12 20v-6M6 20V10M18 20V4"></path>
						</svg>
						<span class="header-btn-label" data-i18n="nav.scale">{t('nav.scale')}</span>
					</button>

					<!-- Share Button -->
					<button id="share-state-btn" type="button" class="header-action-btn" title="Copy Shareable Link" aria-label="Copy Shareable Link">
						<svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
							<path d="M4 12v8a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-8"></path>
							<polyline points="16 6 12 2 8 6"></polyline>
							<line x1="12" y1="2" x2="12" y2="15"></line>
						</svg>
						<span class="share-btn-text header-btn-label" data-i18n="nav.share">{t('nav.share')}</span>
					</button>

					<!-- Print Button -->
					<button id="print-report-btn" type="button" class="header-action-btn header-print-btn" title="Print Grade Audit" aria-label="Print Grade Audit">
						<svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
							<polyline points="6 9 6 2 18 2 18 9"></polyline>
							<path d="M6 18H4a2 2 0 0 1-2-2v-5a2 2 0 0 1 2-2h16a2 2 0 0 1 2 2v5a2 2 0 0 1-2 2h-2"></path>
							<rect x="6" y="14" width="12" height="8"></rect>
						</svg>
						<span class="header-btn-label" data-i18n="nav.print">{t('nav.print')}</span>
					</button>

					<!-- Language Selector -->
					<LanguagePicker />

					<!-- Theme Switcher -->
					<button id="theme-toggle-btn" type="button" class="header-action-btn icon-only" title="Toggle Theme" aria-label="Toggle Theme">
						<svg id="theme-sun-icon" style="display: none;" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
							<circle cx="12" cy="12" r="5"></circle>
							<line x1="12" y1="1" x2="12" y2="3"></line>
							<line x1="12" y1="21" x2="12" y2="23"></line>
							<line x1="4.22" y1="4.22" x2="5.64" y2="5.64"></line>
							<line x1="18.36" y1="18.36" x2="19.78" y2="19.78"></line>
							<line x1="1" y1="12" x2="3" y2="12"></line>
							<line x1="21" y1="12" x2="23" y2="12"></line>
							<line x1="4.22" y1="19.78" x2="5.64" y2="18.36"></line>
							<line x1="18.36" y1="5.64" x2="19.78" y2="4.22"></line>
						</svg>
						<svg id="theme-moon-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
							<path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path>
						</svg>
					</button>
				</div>
			</div>
		</header>

		<!-- Print Header (Hidden on screen, shown in print) -->
		<div class="print-only" style="display: none; padding: 1.5rem 0; border-bottom: 2px solid #000; margin-bottom: 1.5rem;">
			<div style="display: flex; justify-content: space-between; align-items: baseline;">
				<div style="font-size: 1.5rem; font-weight: bold;">Academic Grade Audit Report</div>
				<span style="font-size: 0.875rem; color: #555;">Generated via GradeCalc</span>
			</div>
			<div style="margin-top: 0.5rem; font-size: 0.875rem; color: #333;">
				<span>Date: <span id="print-date"></span></span> | 
				<span>Course: <span id="print-course-name" data-i18n="calc.defaultCourse">{t('calc.defaultCourse')}</span></span>
			</div>
		</div>

		<!-- Main Content -->
		<main style="flex: 1; position: relative;">
			<slot />
		</main>

		<!-- Refined Fuller Site Footer -->
		<footer class="site-footer">
			<div class="container site-footer-container">
				<!-- Main 4-Column Grid -->
				<div class="site-footer-grid">
					<!-- Col 1: Brand & Offline Privacy Statement -->
					<div class="site-footer-brand-col">
						<a href={translatePath('/')} class="site-footer-brand">
							<div class="site-footer-brand-icon">
								<svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
									<path d="M22 10v6M2 10l10-5 10 5-10 5z"></path>
									<path d="M6 12v5c3 3 9 3 12 0v-5"></path>
								</svg>
							</div>
							<span class="site-footer-brand-title">GradeCalculatorFinalX</span>
						</a>
						<p class="site-footer-brand-desc" data-i18n="footer.tagline">
							{t('footer.tagline')}
						</p>
					</div>

					<!-- Col 2: Essential Calculators -->
					<div class="site-footer-col site-footer-calc-col">
						<h4 class="site-footer-heading" data-i18n="footer.calcHeading">{t('footer.calcHeading')}</h4>
						<ul class="site-footer-links">
							<li><a href={translatePath('/#weighted')} class="footer-nav-link" data-tab="weighted" data-i18n="tab.weighted">{t('tab.weighted')}</a></li>
							<li><a href={translatePath('/#final')} class="footer-nav-link" data-tab="final" data-i18n="tab.finalExam">{t('tab.finalExam')}</a></li>
							<li><a href={translatePath('/#gpa')} class="footer-nav-link" data-tab="gpa" data-i18n="tab.gpa">{t('tab.gpa')}</a></li>
							<li><a href={translatePath('/indian-cgpa-calculator')} data-i18n="menu.icgpa">{t('menu.icgpa')}</a></li>
							<li><a href={translatePath('/semester-cgpa-calculator')} data-i18n="menu.sem_cgpa">{t('menu.sem_cgpa')}</a></li>
							<li><a href={translatePath('/target-cgpa-calculator')} data-i18n="menu.target_cgpa">{t('menu.target_cgpa')}</a></li>
							<li><a href={translatePath('/grade-converter')} data-i18n="menu.converter">{t('menu.converter')}</a></li>
						</ul>
					</div>

					<!-- Col 3: Platform & Trust -->
					<div class="site-footer-col site-footer-platform-col">
						<h4 class="site-footer-heading" data-i18n="footer.platformHeading">{t('footer.platformHeading')}</h4>
						<ul class="site-footer-links">
							<li><a href={translatePath('/about')} data-i18n="nav.about">{t('nav.about')}</a></li>
							<li><a href={translatePath('/privacy-policy')} data-i18n="nav.privacy">{t('nav.privacy')}</a></li>
							<li><a href={translatePath('/terms-and-conditions')} data-i18n="nav.terms">{t('nav.terms')}</a></li>
							<li><a href={translatePath('/contact')} data-i18n="nav.contact">{t('nav.contact')}</a></li>
						</ul>
					</div>

					<!-- Col 4: Single Academic Formula Box -->
					<div class="site-footer-formula-col">
						<h4 class="site-footer-heading" data-i18n="footer.formulasHeading">{t('footer.formulasHeading')}</h4>
						<div class="site-footer-formula-box">
							<span class="site-footer-formula-title" data-i18n="footer.formulaTitle">{t('footer.formulaTitle')}</span>
							<code class="site-footer-formula-code">&Sigma;(Score &times; Weight) &divide; &Sigma;Weight</code>
							<p class="site-footer-formula-note" data-i18n="footer.formulaNote">{t('footer.formulaNote')}</p>
						</div>
					</div>
				</div>

				<!-- Bottom Bar: Copyright on left + Meta Links on right -->
				<div class="site-footer-bottom">
					<div class="site-footer-copy">
						&copy; <span id="current-year">2026</span> <span data-i18n="footer.copyright">{t('footer.copyright')}</span>
					</div>
					<div class="site-footer-bottom-links">
						<a href={translatePath('/about')} data-i18n="nav.about">{t('nav.about')}</a>
						<span class="site-footer-dot" aria-hidden="true">&bull;</span>
						<a href={translatePath('/privacy-policy')} data-i18n="nav.privacy">{t('nav.privacy')}</a>
						<span class="site-footer-dot" aria-hidden="true">&bull;</span>
						<a href={translatePath('/terms-and-conditions')} data-i18n="nav.terms">{t('nav.terms')}</a>
						<span class="site-footer-dot" aria-hidden="true">&bull;</span>
						<a href={translatePath('/contact')} data-i18n="nav.contact">{t('nav.contact')}</a>
					</div>
				</div>
			</div>
		</footer>

		<!-- Course Creation Modal -->
		<CourseModal />

		<!-- Client i18n Engine & Theme Toggle -->
		<script>
			import { initClientI18n } from '../i18n/client';

			// Initialize client i18n
			const pageLang = (document.documentElement.lang || 'en') as any;
			initClientI18n(pageLang);

			// Theme Toggle Logic
			const themeToggleBtn = document.getElementById('theme-toggle-btn');
			const sunIcon = document.getElementById('theme-sun-icon');
			const moonIcon = document.getElementById('theme-moon-icon');

			function updateThemeIcons() {
				const isDark = document.documentElement.classList.contains('dark');
				if (sunIcon && moonIcon) {
					if (isDark) {
						sunIcon.style.display = 'block';
						moonIcon.style.display = 'none';
					} else {
						sunIcon.style.display = 'none';
						moonIcon.style.display = 'block';
					}
				}
			}
			updateThemeIcons();

			themeToggleBtn?.addEventListener('click', () => {
				const isDark = document.documentElement.classList.toggle('dark');
				localStorage.setItem('gradecalc_theme', isDark ? 'dark' : 'light');
				updateThemeIcons();
			});

			// Print date setup
			const printDateEl = document.getElementById('print-date');
			if (printDateEl) {
				printDateEl.textContent = new Date().toLocaleDateString(undefined, {
					year: 'numeric',
					month: 'long',
					day: 'numeric'
				});
			}

			// Current year
			const yearEl = document.getElementById('current-year');
			if (yearEl) yearEl.textContent = new Date().getFullYear().toString();

			// Print button
			document.getElementById('print-report-btn')?.addEventListener('click', () => {
				window.print();
			});
		</script>
	</body>
</html>
